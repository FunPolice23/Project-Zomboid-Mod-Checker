# ================================================
# PZ Mod Compatibility Checker - Requirements
# ================================================

# === Runtime dependencies (needed to run the program) ===
PyQt6>=6.5.0
tqdm

# === Build dependencies (needed to create the .exe with Pyinstaller) ===
Pyinstaller
zstandard

# ================================================
# Special installation notes
# ================================================

# kirjava is not on PyPI — install from GitHub:
# pip install git+https://github.com/notiska/kirjava.git

# How to install everything:
# pip install -r requirements.txt
